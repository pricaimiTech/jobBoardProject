type Query {
    jobs: [Job!]
}

type Company {
    id: ID!
    name: String!
    description: String
}
type Job {
    id: ID!
    "this field is a connection with company db"
    company: Company!
    title: String!
    description: String!
    createdAt: String!
}